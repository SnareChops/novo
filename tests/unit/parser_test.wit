// Test interface for parser component
interface parser-test {
    // Test entry point that returns error code (0 for success)
    export test: func() -> u32

    // Get detailed error information
    export get-error: func() -> u32
}

world parser-test-component {
    import memory
    import parser
    export parser-test
}
